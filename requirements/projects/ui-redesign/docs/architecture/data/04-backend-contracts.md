# Backend Contracts


Detailed payloads live in:
- `../../contracts/api/README.md`
- `openapi.json` (full schema)
- `CodingBridge/Generated/*.swift` (generated models)

Ensure StreamEvent parsing stays in sync with these sources (including `ServerMessage` and `StreamServerMessage`).

---
